package interfaces;

// Priority interface defines the structure for managing task priorities
public interface Priority {

	// Method to set the priority of a task
	void setPriority(int priority);

	// Method to get the priority of a task
	int getPriority();
}
